# =============================================================================
# TRAEFIK INGRESSROUTES - Infrastructure Services
# =============================================================================
# ArgoCD IngressRoute with TLS
# Uses default certificate from TLSStore
# =============================================================================

---
# =============================================================================
# ARGOCD - IngressRoute
# =============================================================================
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: argocd
  namespace: argocd
  labels:
    app: argocd
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`argocd.localtest.me`)
      kind: Rule
      services:
        - name: argocd-server
          port: 80
  tls: {}